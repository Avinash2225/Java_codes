package assertionsintestng;

import org.testng.Assert;
import org.testng.annotations.Test;

public class HardAssertios1 {
	
	@Test
	void test() {
		//Assert.assertEquals("abc", "abc");
		//Assert.assertEquals(123, 345);
	//	Assert.assertEquals("abc", 145);
	Assert.assertEquals("adc", 344);	
Assert.assertNotEquals(234, 456);
	
	}
	

}
